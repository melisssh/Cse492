"""
Skeleton module for extracting visual features from video (MediaPipe etc.).

For now this file only contains function stubs. Later it can be extended
with metrics such as facial expressions, gaze direction, and gesture
statistics.
"""

from typing import Dict


def extract_features(video_path: str) -> Dict[str, float]:
    """
    Extract features from the given video file.

    Currently returns an empty dictionary; later this will be populated
    with real features using MediaPipe or similar libraries.
    """
    return {}

